{% comment %} Generic content page (About, Guarantee, Ingredients, Subscription Terms, Contact, etc.) {% endcomment %}
<section class="bf-section bf-bg-white">
  <div class="bf-container bf-max-640">
    <h1>{{ page.title }}</h1>
    <div class="bf-page-content">
      {{ page.content }}
    </div>
  </div>
</section>
{% schema %}
{ "name": "Page content" }
{% endschema %}
